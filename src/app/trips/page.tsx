"use client";import {Shell} from "@/components/shell";import {trips,yen} from "@/lib/data";import {CalendarDays,Camera,MapPin,Plane,Plus} from "lucide-react";import {useState} from "react";
export default function Trips(){const [open,setOpen]=useState(false);return <Shell title="旅行プラン" eyebrow="TRAVEL CONCIERGE"><div className="card trip-banner"><div><div className="eyebrow">NEXT JOURNEY</div><h2 style={{margin:'8px 0'}}>家族で宮古島サマーバケーション</h2><div style={{fontSize:13,color:'#ced8e5'}}>9月2日 — 9月6日 ・ イラフ SUI ・ ANA 1747</div></div><div style={{textAlign:'right'}}><div className="days">58</div><small>DAYS TO GO</small></div></div><div className="grid stats3"><div className="card"><div className="card-label"><CalendarDays size={14}/>今年の旅行</div><div className="metric">5 <small>回</small></div></div><div className="card"><div className="card-label">年間旅行予算</div><div className="metric">{yen(4280000)}</div></div><div className="card"><div className="card-label">利用予定ポイント・マイル</div><div className="metric">420,000 <small>pt</small></div></div></div><section className="card" style={{marginTop:16}}><div className="section-head"><div className="section-title">今後の旅行</div><button className="btn primary" onClick={()=>setOpen(!open)}><Plus size={14} style={{display:'inline',verticalAlign:'middle'}}/> 旅行を登録</button></div>{open&&<div className="notice">旅行登録フォームはSupabase接続後も同じ項目構成で保存されます。デモでは一覧へ即時反映する設計です。</div>}<div className="grid page-grid">{trips.map(t=><article className="card item-card" key={t.name} style={{boxShadow:'none'}}><div className="item-top"><span className="tag">{t.status}</span><Plane size={18}/></div><h3>{t.name}</h3><div className="meta"><MapPin size={12} style={{display:'inline'}}/> {t.place}</div><div className="rule"><span>日程</span><b>{t.from.slice(5).replace('-','/')} — {t.to.slice(5).replace('-','/')}</b></div><div className="rule"><span>ホテル</span><b>{t.hotel}</b></div><div className="rule"><span>予算</span><b>{yen(t.budget)}</b></div><button className="btn" style={{width:'100%',marginTop:12}}><Camera size={14} style={{display:'inline'}}/> 写真・メモ</button></article>)}</div></section></Shell>}
