"use client";import Link from "next/link";import {useSearchParams} from "next/navigation";import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function Login(){const params=useSearchParams();const demo=process.env.NEXT_PUBLIC_REQUIRE_AUTH==="false";const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState(params.get('reason')==='setup'?'Supabaseの設定が必要です。READMEの設定手順をご確認ください。':'');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const sb=createClient();if(!sb){setMessage('Supabaseが設定されていません。');setBusy(false);return}const {error}=await sb.auth.signInWithPassword({email,password});if(error){setMessage('メールアドレスまたはパスワードを確認してください。');setBusy(false)}else location.href='/'}return <div className="login"><section className="login-art"><div><div className="brand-kicker">PERSONAL CONCIERGE</div><h1 style={{fontFamily:'Georgia',fontSize:40}}>KAZUAKI<br/>LIFE HUB</h1><p style={{color:'#bfcbd9'}}>Business / Travel / Points / Camera Dashboard</p></div><p style={{maxWidth:500,lineHeight:1.8}}>仕事も旅も、人生の大切な数字も。<br/>静かに整い、次の一手が見える場所。</p></section><main className="login-box"><div className="eyebrow">WELCOME BACK</div><h2 className="page-title">ログイン</h2><p className="muted" style={{fontSize:13}}>登録済みのメールアドレスでログインしてください。</p><form onSubmit={submit}><label className="card-label">メールアドレス</label><input className="field" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@example.com" autoComplete="email" required/><label className="card-label">パスワード</label><input className="field" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/><button className="btn primary" type="submit" disabled={busy}>{busy?'確認中…':'ログイン'}</button></form>{message&&<div className="notice" role="alert">{message}</div>}{demo&&<Link href="/" className="btn" style={{display:'block',textAlign:'center',marginTop:12}}>デモ画面を見る</Link>}<p className="meta" style={{textAlign:'center',marginTop:25}}>Apple / GoogleログインはPhase 2で追加予定</p></main></div>}
