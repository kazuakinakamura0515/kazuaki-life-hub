"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {Bell,Camera,ChevronRight,CreditCard,Home,Menu,Newspaper,Plane,Settings,WalletCards} from "lucide-react";
const nav=[['/',"ホーム",Home],['/news',"ニュース",Newspaper],['/points',"ポイント・マイル",WalletCards],['/cards',"クレジットカード",CreditCard],['/trips',"旅行",Plane],['/camera',"カメラ・SONY",Camera],['/settings',"設定",Settings]] as const;
export function Shell({children,title,eyebrow="KAZUAKI LIFE HUB"}:{children:React.ReactNode,title:string,eyebrow?:string}){const path=usePathname();return <div className="shell"><aside className="sidebar"><div className="brand"><div className="brand-kicker">PERSONAL CONCIERGE</div><div className="brand-title">KAZUAKI LIFE HUB</div></div><nav className="nav">{nav.map(([href,label,Icon])=><Link key={href} className={path===href?'active':''} href={href}><Icon size={17}/>{label}</Link>)}</nav><div className="side-foot"><div className="profile-mini"><span className="avatar">K</span><div><b style={{fontSize:12}}>Kazuaki</b><div style={{fontSize:10,color:'#8e9bad'}}>ANA Diamond</div></div><ChevronRight size={14} style={{marginLeft:'auto'}}/></div></div></aside><main className="main"><header className="topbar"><div><div className="eyebrow">{eyebrow}</div><h1 className="page-title">{title}</h1></div><div className="top-actions"><button className="icon-btn" aria-label="通知"><Bell size={18}/></button><button className="icon-btn" aria-label="メニュー"><Menu size={18}/></button></div></header>{children}</main><nav className="mobile-nav">{nav.slice(0,5).map(([href,label,Icon])=><Link key={href} className={path===href?'active':''} href={href}><Icon size={20}/>{label.replace('クレジット','カード')}</Link>)}</nav></div>}
export function SectionHead({title,action="すべて見る"}:{title:string,action?:string}){return <div className="section-head"><div className="section-title">{title}</div><button className="btn" style={{fontSize:11,padding:'7px 10px'}}>{action}</button></div>}
